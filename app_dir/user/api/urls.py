from django.urls import path

from app_dir.user.api import views

urlpatterns = [
    # path('users/', views.UserList.as_view(), name='user-list'),
    # path('users/<int:pk>/', views.UserDetail.as_view()),
]
